<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <div style="text-align: center; margin-bottom: 20px;">
        <a href="/posts/new">+ Create New Post</a>
    </div>
    
    <% for (post of posts) { %>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <p class="content"><%= post.content %></p>
            <div style="margin-top: 15px;">
                <a href="/posts/<%=post.id%>" style="display: inline-block; margin-right: 10px;">View Details</a>
                <a href="/posts/<%=post.id%>/edit" style="display: inline-block; margin-right: 10px;">Edit</a>
                <form method="post" action="/posts/<%= post.id%>?_method=DELETE" style="display: inline;">
                    <button type="submit" class="delete-btn" onclick="return confirm('Are you sure?')">Delete</button>
                </form>
            </div>
        </div>
    <% } %>
</body>
</html>